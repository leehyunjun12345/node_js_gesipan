<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>글 수정</title>
</head>
<body>
  <h1>글 수정</h1>
  <form action="/posts/<%= post.id %>/edit" method="post">
    <p>제목: <input type="text" name="title" value="<%= post.title %>" required></p>
    <p>내용: <textarea name="content" required><%= post.content %></textarea></p>
    <button type="submit">수정 완료</button>
  </form>
  <a href="/posts/<%= post.id %>">취소</a>
</body>
</html>
